diff --git a/SDL_ttf.c b/SDL_ttf.c
index 404e00b..35cfa4b 100644
--- a/SDL_ttf.c
+++ b/SDL_ttf.c
@@ -114,7 +114,7 @@ int TTF_SetScript(int script) /* hb_script_t */
 }
 
 /* Round glyph to 16 bytes width and use SSE2 instructions */
-#if defined(__SSE2__)
+#if defined(__SSE2__) && !defined(SDL_DISABLE_SSE2_INTRINSICS)
 #  define HAVE_SSE2_INTRINSICS 1
 #endif
 
