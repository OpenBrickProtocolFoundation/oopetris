diff --git a/deflate.c b/deflate.c
index d7d2c7c..4eaa3f6 100644
--- a/deflate.c
+++ b/deflate.c
@@ -1647,6 +1647,10 @@ local void check_match(deflate_state *s, IPos start, IPos match, int length) {
 /* Maximum stored block length in deflate format (not including header). */
 #define MAX_STORED 65535
 
+#if defined(__UEFI__)
+#undef MIN
+#endif
+
 /* Minimum of a and b. */
 #define MIN(a, b) ((a) > (b) ? (b) : (a))
 
