diff --git a/test/meson.build b/test/meson.build
index 8e34b3f..c79ee88 100644
--- a/test/meson.build
+++ b/test/meson.build
@@ -5,12 +5,18 @@ test_files = {
     'flags test': files('test_flags.cpp'),
 }
 
+test_deps = []
+if host_machine.system() == 'android'
+    cpp = meson.get_compiler('cpp')
+    test_deps += [cpp.find_library('log')]
+endif
+
 foreach test_name, test_src : test_files
     test_exe = executable(
         test_name.underscorify(),
         test_src,
 
-        dependencies: [magic_enum_dep, catch2_dep],
+        dependencies: [magic_enum_dep, catch2_dep, test_deps],
     )
 
     test(test_name, test_exe)
