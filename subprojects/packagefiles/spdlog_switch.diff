diff --git a/meson.build b/meson.build
index 5bd50e5..a318f59 100644
--- a/meson.build
+++ b/meson.build
@@ -14,10 +14,24 @@ thread_dep = dependency('threads')
 spdlog_dependencies = [thread_dep]
 spdlog_compile_args = []
 
-fmt_dep = dependency('fmt', version: '>=8.1', required: get_option('external_fmt'))
-if fmt_dep.found()
-  spdlog_dependencies += fmt_dep
-  spdlog_compile_args += '-DSPDLOG_FMT_EXTERNAL'
+if (meson.is_cross_build() and host_machine.system() == 'switch')
+    fmt_dep = dependency(
+        'fmt_header_only',
+        version: '>=8.1',
+        required: true
+    )
+    spdlog_dependencies += fmt_dep
+    spdlog_compile_args += '-DSPDLOG_FMT_EXTERNAL'
+else
+    fmt_dep = dependency(
+        'fmt',
+        version: '>=8.1',
+        required: get_option('external_fmt'),
+    )
+    if fmt_dep.found()
+        spdlog_dependencies += fmt_dep
+        spdlog_compile_args += '-DSPDLOG_FMT_EXTERNAL'
+    endif
 endif
 
 if host_machine.system() == 'android'
