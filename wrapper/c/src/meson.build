recordings_include_dir = include_dir / 'oopetris'

install_headers(
    files('oopetris_wrapper.h'),
    install_dir: recordings_include_dir,
    preserve_path: true,
)

src_files += files('wrapper.cpp')
